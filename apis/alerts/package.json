{
  "name": "peakpost-alerts",
  "version": "0.1.0",
  "description": "Multi-channel alert fan-out for autonomous Peak Post services.",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
