{
  "name": "peakpost-health",
  "version": "0.1.0",
  "description": "15-minute self-monitoring cron for Peak Post autonomous services.",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.1"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
