{
  "name": "dotenv-preview",
  "version": "1.0.0",
  "description": "Chrome extension that syntax-highlights .env files on GitHub with grouping, sensitive value detection, and copy buttons",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "jsdom": "^26.0.0"
  }
}
